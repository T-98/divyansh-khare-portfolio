[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    live: hits the real OpenAI API; requires OPENAI_API_KEY and is skipped by default
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
